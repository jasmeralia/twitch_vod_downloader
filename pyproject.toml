[project]
# Runtime dependencies for native TrueNAS cron + uv (Odoo #472, part of epic #466).
# requirements.txt remains the source of truth for the Docker image build; keep both in sync.
name = "twitch-vod-downloader"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "yt-dlp",
    "pydantic-settings>=2.15.0",
]

[tool.uv]
package = false
